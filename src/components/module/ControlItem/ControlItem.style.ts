import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    // Container style
    borderRadius: 10,
    backgroundColor: '#fff',
    paddingVertical: 24,
    paddingHorizontal: 8,
    elevation: 2,
    flexDirection: 'row',
    alignItems: 'center',
  },
  labelContainer: {
    flex: 1,
    marginHorizontal: 8,
  },
});

export default styles;
